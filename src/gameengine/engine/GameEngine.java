package gameengine.engine;

public class GameEngine {
    private GameLoop gameLoop = new GameLoop();
    //initialize a JSONParser
    
    
    /**
     *When the user chooses a file, created from the GAE, this method iterates through the data, which is parsed
     *by JSONParser, and initialize all the patches
     *all the patches 
     */
    public void initializePatches(){
        
    }
    
    /**
     * When the user chooses a file, created from the GAE, this method iterates through the data, which is parsed
     * by JSONParser, and initialize all the pieces
     */
    public void initializePieces(){
        
    }
    
    
}
